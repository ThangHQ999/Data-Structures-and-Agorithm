#include <bits/stdc++.h>
using namespace std;
int main() {
    ifstream fileIn;
    int n, w;
    cin>> n >> w;
    int a[n], c[n];
    for (int i=0; i<n; i++) {
        cin>> c[i];
    }
    for (int i=0; i<n; i++) {
        cin>> a[i];
    }
    
    return 0;
}