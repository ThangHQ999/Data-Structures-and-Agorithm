#include <bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin>> t;
    while (t--)
    {
        int n;
        cin>> n;
        int x, y, z;
        cin>> x >> y >> z;
        vector<long long> dp(n+1, 1e9);
        dp[0] = x;
        for (int i=2; i<n; i++) {
            
        }
    }
    
    return 0;
}