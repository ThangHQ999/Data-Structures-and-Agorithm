#include <bits/stdc++.h>
using namespace std;
string p;

void Try(string s) {
    
}

void solve() {
    cin>> p;
    Try(p);
}
int main() {
    int t;
    cin>> t;
    while (t--)
    {
        solve();
    }
    
    return 0;
}